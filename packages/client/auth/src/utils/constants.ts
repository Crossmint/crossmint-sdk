import { CROSSMINT_API_VERSION } from "@crossmint/common-sdk-auth";

export const TIME_BEFORE_EXPIRING_JWT_IN_SECONDS = 120;
export const AUTH_SDK_ROOT_ENDPOINT = `api/${CROSSMINT_API_VERSION}/session/sdk/auth`;
