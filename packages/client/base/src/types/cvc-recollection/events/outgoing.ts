// The hosted page owns the CVC field and the Confirm button, so the parent never
// sends it anything.
export const cvcRecollectionOutgoingEvents = {};
export type CvcRecollectionOutgoingEventMap = typeof cvcRecollectionOutgoingEvents;
