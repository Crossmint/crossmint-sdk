import { SdkLogger } from "./SdkLogger";

/**
 * Singleton instance of the SDK Logger
 * This is the main entry point for logging throughout the SDK
 */
export const SdkLoggerInstance = new SdkLogger();
