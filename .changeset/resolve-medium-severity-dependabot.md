---
---

Resolve medium severity Dependabot advisories by bumping pnpm overrides for `postcss`, `undici`, `tar`, `valibot`, `protobufjs`, `markdown-it`, `ajv@8`, `morgan` and `esbuild`. Document unfixable `react-router`/`react-router-dom` v6 and `@hey-api/openapi-ts` advisories.
