---
"@crossmint/client-sdk-react-native-ui": minor
---

Added `CrossmintIdentityVerification` to the React Native SDK. It renders Crossmint's hosted identity verification step in a WebView from an order's `payment.preparation.kyc` credentials, and reports `onReady`, `onComplete`, `onCancel` and `onError`.
