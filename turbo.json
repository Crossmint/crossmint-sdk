{
    "$schema": "https://turbo.build/schema.json",
    "tasks": {
        "build": {
            "outputLogs": "new-only",
            "dependsOn": ["^build"]
        },
        "test:circular": {},
        "test:vitest": {
            "outputLogs": "new-only"
        }
    }
}
