{
    "$schema": "https://turbo.build/schema.json",
    "tasks": {
        "build": {
            "env": ["NODE_ENV"],
            "outputLogs": "new-only",
            "dependsOn": ["^build"]
        },
        "test:vitest": {
            "outputLogs": "new-only"
        }
    }
}
